import styled from 'styled-components';

export const Container = styled.div`
  button {
    display: flex;
    align-items: center;
    gap: 8px;
    outline: none;
    background: transparent;
    border: none;

    span {
      color: #fff;
    }
  }
`;

import styled from 'styled-components';

export const Form = styled.form`
  #saldoInicial {
    font-size: 12px;
    color: #fff;
    font-weight: bold;
    letter-spacing: -0.5px;
  }

  .valueTransaction {
    display: flex;
    align-items: center;
    gap: 8px;

    #RS {
      font-size: 18px;
      color: #fff;
      letter-spacing: -0.5px;
    }
  }

  .inputs {
    margin-top: 10px;
  }

  button {
    width: 100%;
    margin-top: 28px;
  }
`;
